import { ChatView, ChatState, Message } from "views/chat-view.slint";

export { ChatState }

export component AppWindow inherits Window {
    width: 600px;
    height: 500px;

    chat_view := ChatView { }
}
